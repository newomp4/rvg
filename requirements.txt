PyQt6>=6.7
auto-editor>=27.0
Pillow>=10.4
numpy>=1.26
imageio-ffmpeg>=0.5
# Qwen3-TTS — local TTS with built-in preset voices (no reference clip required).
qwen-tts
# Forced alignment for word-level caption timings — Qwen doesn't expose them.
whisper-timestamped>=1.15
torch>=2.4
torchaudio>=2.4
soundfile>=0.12
# Headless Chromium for rendering the title card as a real motion graphic
# (HTML/CSS animations + per-frame screenshots) instead of PIL per-frame.
playwright>=1.45
# Anthropic API client for the "generate title" button.
anthropic>=0.40
